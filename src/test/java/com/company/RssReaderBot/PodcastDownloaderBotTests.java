package com.company.RssReaderBot;

import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

@SpringBootTest
class PodcastDownloaderBotTests {

    /*@Test
    void createInlineKeyBoardMessageTest() {
        createInlineKeyBoard(1, List.of("1"), List.of("1"));
        createInlineKeyBoard(3, asList("1", "2", "3"), asList("1", "2", "3"));
        createInlineKeyBoard(1, List.of("1"), asList("1", "1")); // exception
        createInlineKeyBoard(2, List.of("1"), asList("1", "2")); // exception
        createInlineKeyBoard(3, List.of("1"), List.of("1")); // exception
    }*/

}
